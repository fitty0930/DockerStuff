version: '3'
services:
  app:
    image: dimater-img
    container_name: dimater-container
    build: ./
    restart: always
    ports:
      - "108:80"
    volumes:
      - .:/var/www/html
  myadmin:
    image: phpmyadmin
    container_name: dimater-phpmyadmin
    restart: always
    depends_on:
      - mysql
    ports:
      - "109:80"
    links:
      - mysql:db
  # mysql:
  #   container_name: marcantonio-mysql
  #   image: mysql:5.6
  #   restart: always
  #   ports:
  #     - 3308:3306
  #   volumes:
  #     - mysql:/var/lib/mysql
  #   environment:
  #     MYSQL_HOST: 0.0.0.0
  #     MYSQL_ROOT_PASSWORD: Rut123.
volumes:
    mysql:
      driver: "local"
networks:
  default:
    name: wxpanel_allnet